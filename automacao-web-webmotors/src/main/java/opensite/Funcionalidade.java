package opensite;

import org.junit.After;
import org.junit.Before;
import org.junit.Test;
import org.openqa.selenium.By;
import org.openqa.selenium.Keys;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;

public class Funcionalidade {

	private static WebDriver driver;



	@Test
	
	public static void clicaVerOfertas(){

	}
	
		// WebElement passBox = driver.findElement(By.name("fun_Senha"));
		// try {
		// FileReader arq = new FileReader("senha.txt");
		// BufferedReader lerArq = new BufferedReader(arq);
		// String linha = lerArq.readLine();
		// passBox.sendKeys(linha);
		// passBox.sendKeys(Keys.ENTER);
		// Thread.sleep(3000);
		// arq.close();
		// } catch (IOException e) {
		// System.err.printf("Erro na abertura do arquivo: %s.\n",
		// e.getMessage());
		// }
		// WebElement inserirRequerimento =
		// driver.findElement(By.xpath("//a@[href='requerimentoCad.php?v=MDI4Mzl8cmVxX0RhdGE9MTAvMTIvMjAxOWNyeXB0']"));
		// inserirRequerimento.click();
		// driver.findElement(By.xpath("//a[@href='/docs/configuration']")).click();
	}
